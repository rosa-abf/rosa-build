en:
  layout:
    hooks:
      services:
        web: WebHook URLs
        hipchat: HipChat

  flash:
    hook:
      save_error: Unable to save hook
      created: Hook was successfully created
      updated: Hook was successfully updated

  activerecord:
    attributes:
      hook:
        data:
          url: URL
          auth_token: Auth Token
          room: Room
          restrict_to_branch: Restrict To Branch
          notify: Notify
