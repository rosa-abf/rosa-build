.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to t("layout.issues.list"), @issue ? project_issue_path(@project, @issue) : commit_path(@project, @commit)
  .content
    %h2.title
      = t("layout.issues.edit_header")
    .inner
      = form_for @comment, :url => @update_url, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
